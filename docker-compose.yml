services:
  pi-clock:
    build: .
    image: pi-clock-display:local
    container_name: pi-clock-display
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      CONFIG_PATH: /app/data/config.json
      PHOTO_CACHE_DIR: /app/data/cache/photos
      LOCAL_PHOTO_DIR: /app/data/photos
    volumes:
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/healthz').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
